The replay's map half, aimed correctly. v0.4.0's camera fitted its bounds
while the dialog was still animating open, so the colony could sit outside
the visible pane.

Карта в реплее теперь наведена верно. В v0.4.0 камера подгоняла границы,
пока диалог ещё анимировался при открытии, и колония могла оказаться за
пределами видимой области.

## What's Changed
### Bug Fixes
- Fix the replay map aiming past the colony: the bounds were fitted against a half-sized pane during the dialog's opening animation, and resizing alone never re-aimed the camera — the fit is now repeated once the pane has settled

## Что изменилось
### Исправления
- Исправить прицел карты реплея мимо колонии: границы подгонялись по полуразмерной панели во время анимации открытия диалога, а один только resize камеру не перенаводил — теперь подгонка повторяется после стабилизации панели
