## What's Changed
### Bug Fixes
- Fix manual pinning on the map: the click handler captured a stale flag at startup and ignored every pin click since the platform was born
- Fix the photo flow never reaching Confirm: a location-less photo is now held as pending, pin mode arms itself, and a single pinned point is enough for a still

## Что изменилось
### Исправления
- Исправить ручную установку точек на карте: обработчик клика замораживал флаг при старте и игнорировал каждый клик с самого рождения платформы
- Исправить недостижимость кнопки Confirm для фото: снимок без локации теперь удерживается как ожидающий, режим точек включается сам, а для фото достаточно одной точки
